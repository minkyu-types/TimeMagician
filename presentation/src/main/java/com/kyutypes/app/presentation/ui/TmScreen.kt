package com.kyutypes.app.presentation.ui

enum class TmScreen {
    Home,
    Convert,
    Result
}

/*
 1. Sealed 클래스의 서브 클래스들은 반드시 같은 파일 내에 선언되어야 함
 2. Sealed 클래스는 기본적으로 abstract 클래스임
 3. Sealed 클래스는 private 생성자만 가짐
 */
